// Minimal project entrypoint (placeholder).
// This file is intentionally lightweight — it prints a confirmation message and exits.

console.log('Server placeholder: run "npm start" to execute server/index.js');
process.exit(0);
